{
  "_doc": "The weekly scout report: what exists out there that this box is not using. Written by the Claude routine described in docs/models.md and read by src/lib/model-scout.ts. Judgment lives here; facts about which model ids a vendor currently sells are NOT — those are pulled live from each vendor's model-list endpoint at read time, because a list committed to git is out of date the day after it is written. Fit verdicts are likewise computed at read time against the live machine, never stored here: a verdict depends on what is running and how much disk is free, and both change hourly.",
  "_schema": "candidates[]: { id, name, kind: local|cloud, capability, why, checkpoint?, provider?, target?, mode?, params?, released?, license?, docs?, paper?, requirement? { vramGb, ramGb, diskGb, basis, estimated }, spec? { paramsB, precision, contextK, kind }, pricing? }. Give `requirement` when the authors published usable numbers; give `spec` otherwise and let estimateFromParams() do the arithmetic with an honest basis string. upgrades[]: { alias, from, to, why } — a like-for-like repoint of an alias already in ai-router.yaml.",
  "generatedAt": "2026-08-19",
  "generatedBy": "initial run, by hand — Hugging Face hub search plus live vendor model lists; text models delegated to the Leaderboard tab",
  "candidates": [
    {
      "id": "z-image-turbo",
      "name": "Z-Image-Turbo",
      "kind": "local",
      "capability": "image",
      "checkpoint": "Tongyi-MAI/Z-Image-Turbo",
      "params": "6.2B",
      "released": "2025-11",
      "license": "apache-2.0",
      "docs": "https://hf.co/Tongyi-MAI/Z-Image-Turbo",
      "paper": "https://arxiv.org/abs/2511.22699",
      "spec": {
        "paramsB": 6.2,
        "precision": "bf16",
        "kind": "diffusion"
      },
      "why": "The single biggest win available to this box. local-qwen-image is 20.4B and holds ~28 GB of host RAM for as long as its service is loaded — the collision that has already killed it mid-load. Z-Image-Turbo is a third the size at 6.2B, Apache-2.0, and by far the most-pulled open image model on the hub (8.1M downloads), so it would leave room for a FLUX run or a 27B chat model beside it instead of owning the machine."
    },
    {
      "id": "flux2-klein-4b",
      "name": "FLUX.2 klein 4B",
      "kind": "local",
      "capability": "image",
      "checkpoint": "black-forest-labs/FLUX.2-klein-4B",
      "params": "3.9B transformer",
      "released": "2026-01",
      "license": "apache-2.0",
      "docs": "https://hf.co/black-forest-labs/FLUX.2-klein-4B",
      "requirement": {
        "vramGb": 15,
        "ramGb": 8,
        "diskGb": 14,
        "estimated": true,
        "basis": "Estimated, and deliberately generous. The 3.9B on the model card is the TRANSFORMER only: 3.9B x 2 bytes = 7.8 GB at bf16. FLUX.2 klein also loads a Qwen3-based text encoder that the card does not size, and the community's uncensored-encoder repos for the klein family are large enough that ignoring it would understate this badly — so ~6 GB is allowed for the encoder plus VAE on top. Verify against a real run before trusting the number.",
        "estimatedNote": "Replace this whole block with a measurement once it has been run once."
      },
      "why": "Direct replacement for local-flux-schnell, which measured 31.7 GB of a 31.8 GB card during a 512x512 run — it effectively IS the card, and nothing fits beside it. klein-4B is Apache-2.0, does text-to-image and image editing in one checkpoint, and is a fifth of the size. 2.3M downloads."
    },
    {
      "id": "qwen3-asr-1.7b",
      "name": "Qwen3-ASR 1.7B",
      "kind": "local",
      "capability": "stt",
      "checkpoint": "Qwen/Qwen3-ASR-1.7B",
      "params": "1.7B",
      "released": "2026-01",
      "license": "apache-2.0",
      "docs": "https://hf.co/Qwen/Qwen3-ASR-1.7B",
      "spec": {
        "paramsB": 1.7,
        "precision": "fp16",
        "kind": "audio"
      },
      "why": "local-whisper is large-v3, released November 2023 and the oldest checkpoint on this box. Qwen3-ASR is current, Apache-2.0, similar size, and pulled 4.4M times. The local STT path exists so sensitive audio never leaves the machine, which makes its accuracy worth keeping current."
    },
    {
      "id": "nemotron-3.5-asr-streaming-0.6b",
      "name": "Nemotron 3.5 ASR streaming 0.6B",
      "kind": "local",
      "capability": "stt",
      "checkpoint": "nvidia/nemotron-3.5-asr-streaming-0.6b",
      "params": "0.6B",
      "released": "2026-05",
      "license": "nvidia-open-model",
      "docs": "https://hf.co/nvidia/nemotron-3.5-asr-streaming-0.6b",
      "spec": {
        "paramsB": 0.6,
        "precision": "fp16",
        "kind": "audio"
      },
      "why": "Cache-aware streaming, which whisper-large-v3 cannot do at all — it transcribes finished audio. Rounding error on this card at 0.6B, so it could run permanently alongside everything else and give the console live transcription rather than batch."
    },
    {
      "id": "higgs-tts-3-4b",
      "name": "Higgs TTS 3 (4B)",
      "kind": "local",
      "capability": "tts",
      "checkpoint": "bosonai/higgs-tts-3-4b",
      "params": "4B",
      "released": "2026-06",
      "license": "apache-2.0",
      "docs": "https://hf.co/bosonai/higgs-tts-3-4b",
      "spec": {
        "paramsB": 4,
        "precision": "bf16",
        "kind": "audio"
      },
      "why": "local-kokoro is 82M and sounds like it. Higgs 3 is expressive, controllable and does voice cloning across ~100 languages at 4B — still small enough to co-reside with an image model. The upgrade to make if anything on this box is meant to be listened to rather than checked."
    }
  ],
  "upgrades": [
    {
      "alias": "claude-opus",
      "from": "anthropic/claude-opus-4-8",
      "to": "anthropic/claude-opus-5",
      "why": "claude-opus-5 shipped 2026-07-24 and claude-opus-4-8 on 2026-05-28. Same tier, same alias, two generations on — a like-for-like repoint."
    },
    {
      "alias": "claude-sonnet",
      "from": "anthropic/claude-sonnet-4-6",
      "to": "anthropic/claude-sonnet-5",
      "why": "claude-sonnet-5 shipped 2026-06-29; the wired claude-sonnet-4-6 dates from 2026-02-17. The sonnet alias has been four months stale."
    },
    {
      "alias": "gemini-flash",
      "from": "gemini/gemini-3.6-flash",
      "to": "gemini/gemini-3.7-flash",
      "why": "gemini-3.7-flash is live on the v1beta ListModels endpoint. Google publishes no release dates, so this is a version-number judgement rather than a dated one — but the flash line moves fast and the wired version is one behind."
    },
    {
      "alias": "openai-transcribe",
      "from": "openai/gpt-4o-transcribe",
      "to": "openai/gpt-transcribe",
      "why": "gpt-transcribe appeared 2026-07-27, dropping the 4o generation marker the way gpt-image did. gpt-4o-transcribe dates from 2025-03. Note gpt-live-transcribe shipped the same day for streaming, which the router's audio_transcription mode does not front."
    }
  ],
  "notes": [
    "gemini-pro stays pinned to gemini-3.1-pro-preview on purpose. Google publishes gemini-pro-latest as a moving alias, and ai-router.yaml pins explicit versions so an archived result records which model actually produced it. Do not 'upgrade' it to a -latest id.",
    "Both wired Gemini image models still report the 429 quota error recorded in model-meta.json. That is a billing state on the key, not a model choice — no scouting will fix it.",
    "The image candidates here are both about the same thing: local-qwen-image and local-flux-schnell each need most of this machine, so the console can only ever run one at a time. Two 4-6B models would change what the box can do more than any single upgrade would.",
    "Krea-2-Turbo (12.8B) is trending hard and would fit, but the repo is gated and the licence is bespoke. Left off the candidate list until someone has actually read the terms.",
    "Text LLMs are deliberately absent from this list. The Leaderboard tab ranks every open-weights text model with a published parameter count against this card automatically, and cloud discovery carries live prices — a hand-written candidate there would go stale beside data that does not. What remains is what llm-stats.com does not track: image, speech-to-text and text-to-speech."
  ]
}
