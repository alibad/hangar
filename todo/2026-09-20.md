# 2026-09-20 — AI Tinkerers event readiness

## What changed

- Reconciled the identity: **Hangar** is the product, **Console** is the
  operator surface, and B5/BeTenshi are host profiles. Kept historical private
  repository and Vercel slugs so event prep does not manufacture a risky rename.
- Removed localhost-only manager assumptions from deployed routes and applied
  Cloudflare Access headers to the manager path.
- Made Ollama discovery reflect installed tags and live capabilities. Missing
  configured tags now say **not installed** and are excluded from the default
  this-machine view.
- Stopped charging 20 GB merely because the Ollama runtime is healthy. Resident
  memory appears only when the model is actually loaded.
- Added status, latency, and real target attribution for chat and service
  controls in Requests; filtered dashboard polling from operator history.
- Made production builds use an in-memory DuckDB so a running local console
  cannot make `next build` emit misleading lock errors.
- Added a deterministic event rehearsal and a 15-minute runbook with reset and
  fallback branches.

## Verified locally on B5

- 112 tests pass, TypeScript passes, and the production build completes.
- Manager-owned Ollama stops and starts within the bounded control path.
- `qwen3.8:27b-mlx` returns the staged prompt through `/api/chat`.
- The model is then reported as the 18.2 GB resident occupant.
- Requests shows stop, start, and chat with target Ollama, status 200, and
  measured latency.

## Deployment boundary

- Canonical private repository: `alibad/hangar`.
- Canonical Vercel project: `hangar`.
- Hosted product URLs: `hangar.humanquest.net` (canonical) and
  `console.humanquest.net`.
- `PUBLIC_DOMAIN=betenshi.com` is set for production, `main` previews, and
  development so service URLs no longer resolve to `example.com`.
- The Vercel project's Git production branch still reports the historical
  `master`; this release is promoted explicitly from the built `main` revision.
- Remote machine controls remain conditional on an authenticated manager
  tunnel. Local B5 machine control is the verified event path.

## Deliberately not done

- No repository visibility change, external announcement, or attendee message.
- No public manager tunnel was created without an authenticated Access policy.
- No destructive history rewrite or force push.

## Hangar identity and hosted-mode follow-up

- Renamed the canonical private repository from `alibad/betenshi-console` to
  `alibad/hangar` and the local checkout from `betenshi-console` to `hangar`.
- Verified the archived publication rewrite has no rewrite-only tracked files
  and no commit subjects absent from the canonical history. Its differences are
  sanitization plus older file versions, not unique product work.
- Reinstalled the macOS launch agents from the renamed checkout so local
  startup paths point at `Code/GitHub/ai/hangar`.
- Added an explicit hosted runtime. Storage, persistent image output, local
  request history, assistant usage, model installation/configuration, and MCP
  stdio access now show a truthful **Local machine required** surface on Vercel.
- Selected `https://hangar.humanquest.net` as the canonical hosted URL, with
  `https://console.humanquest.net` as a second entry point.
